#!/bin/bash

DOTFILE_PROJECTS=(
    "i3"
    "polybar"
    "zsh"
    "tmux"
    "ghostty"
    "nvim"
    "picom"
)

for project in "${DOTFILE_PROJECTS[@]}"; do
    echo "Installing $project configuration..."
    stow -D "$project"
    stow -v "$project"
done

